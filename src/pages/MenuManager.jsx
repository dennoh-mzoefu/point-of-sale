import React from "react";

function MenuManager() {
  // add , update, delete,  menu items
  // see if menu item is out of stock
  return (
    <div>
      {/* table that displays menu details in editable inputs and an update button, and a delete button  */}

      <AddStock className="w-full" />
    </div>
  );
}

export default MenuManager;
